# Validación de Workflows

> Validation de Workflows
E2E: entrada → proceso → salida.

Cómo testear LangGraph workflows completos de forma confiable.

---

## 🎯 Objetivo

Escribir tests que:
- ✅ Validen que los nodos se ejecutan en orden
- ✅ Comprueben que el state se actualiza correctamente
- ✅ Muestren que las aristas condicionales enrutan bien
- ✅ No dependan de API real (use mocks)

---

## 🧪 5 Tests Esenciales

| # | Test | Qué Valida | Por Qué | Status |
|----|------|-----------|---------|--------|
| 1 | Nodo Se Ejecuta | Node.call_count == 1 | Si no ejecuta, workflow no funciona | ✅ |
| 2 | State Se Muta | state.get(key) == value | El state es el corazón de LangGraph | ✅ |
| 3 | Cadena de Nodos | n1 → n2 → state.step == 2 | Los workflows son cadenas | ✅ |
| 4 | Arista Condicional | router(state) == expected_node | Decisiones deben ser determinísticas | ✅ |
| 5 | Manejo de Errores | pytest.raises(ValueError) | Workflows deben fallar gracefully | ✅ |

---

## 🏗️ Estructura de Workflow Mock

```python
class MockState:
    """Simula state de LangGraph"""
    def set(self, key, value):
        self.data[key] = value

    def get(self, key, default=None):
        return self.data.get(key, default)

    def update(self, updates):
        self.data.update(updates)

class MockNode:
    """Simula nodo de LangGraph"""
    def __init__(self, name, output=None):
        self.name = name
        self.output = output or {}
        self.call_count = 0

    def execute(self, state):
        self.call_count += 1
        return self.output
```

---

## 📖 Código de Ejemplo

```bash
# Ver test suite
pytest python/validation/test_workflows.py -v

# Ejecutar solo tests de nodos
pytest python/validation/test_workflows.py -v -k "node"

# Ejecutar ejemplo
python python/validation/examples/03_validate_workflow.py
```

---

## ✅ Checklist: Tests de Workflows

- [ ] ¿Cada nodo se ejecuta?
- [ ] ¿El state se actualiza?
- [ ] ¿Las aristas enrutan correctamente?
- [ ] ¿Los errores se manejan?
- [ ] ¿Uso mocks para no llamar APIs?
- [ ] ¿Los tests corren en < 1 segundo?
- [ ] ¿Cubro happy path + error cases?

---

## 🎓 Próximo Paso

Ve a: **[E2E Testing](/03-validacion/03-e2e-testing)** para testear flujos completos de principio a fin.
