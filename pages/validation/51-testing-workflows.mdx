# Validación de Workflows

## Testing LangGraph workflows

```python
def test_workflow():
    result = workflow.invoke({"input": "test"})
    assert result["output"] is not None
    assert result["status"] == "completed"

test_workflow()  # ✅
```

---
**Duración:** 90 min | **Dificultad:** ⭐⭐
