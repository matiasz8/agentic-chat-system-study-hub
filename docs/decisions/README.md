# Decisions

One decision per file, named `NNNN-short-title.md`, numbered in the order taken.
Append-only: when a decision is reversed, add a new file that says so and link back
rather than editing history.

Keep them short. The valuable part is the **why**, and specifically the options that
were rejected — that is the part nobody can reconstruct later.
